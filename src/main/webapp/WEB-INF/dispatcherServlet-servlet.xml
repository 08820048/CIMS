<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 配置 控制器组件 的扫描 -->
    <context:component-scan base-package="cn.edu.hzvtc.controller"></context:component-scan>

    <!-- 配置视图解析器-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/"></property>
        <property name="suffix" value=".jsp"></property>
    </bean>

    <!-- 配置静态资源的访问 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="10485760"></property>
    </bean>

    <mvc:interceptors>
        <!--登录抵截器-->
        <mvc:interceptor>
        <mvc:mapping path="/**"/>
        <bean class="cn.edu.hzvtc.interceptor.LoginInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>


    <mvc:default-servlet-handler></mvc:default-servlet-handler>
    <mvc:annotation-driven></mvc:annotation-driven>
    <mvc:view-controller path="/area/areaIndex" view-name="area/areaIndex"></mvc:view-controller>
    <mvc:view-controller path="/unit/unitIndex" view-name="unit/unitIndex"></mvc:view-controller>
    <mvc:view-controller path="/msg/msgIndex" view-name="msg/msgIndex"></mvc:view-controller>
</beans>